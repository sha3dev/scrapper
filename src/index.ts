/**
 * A wrapper library for puppeteer
 *
 * @remarks
 * The scrapper package consists on a serie of methods that wraps puppeteer
 * native functions. The motivation to build this package is to
 * concentrate all the puppeteer knowledge in a single library and keep a single
 * reference to the puppeteer npm package along all the project
 *
 * @packageDocumentation
 */

import Scrapper from "./lib/scrapper";

export { Scrapper };
